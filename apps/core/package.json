{
  "name": "@agent-metrics/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/server.ts",
    "test": "vitest run src"
  },
  "dependencies": {
    "@agent-metrics/adapters-claude": "workspace:*",
    "@agent-metrics/event-schema": "workspace:*",
    "@agent-metrics/export-kit": "workspace:*",
    "@agent-metrics/metrics-engine": "workspace:*",
    "@agent-metrics/shared-utils": "workspace:*",
    "better-sqlite3": "^11.10.0",
    "fastify": "^5.3.3"
  }
}
